[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "manim-video-api-service"
version = "0.1.0"
description = "A multi-agent Manim video generation API service"
requires-python = ">=3.11"
